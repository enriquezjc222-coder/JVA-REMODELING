/*
  Legacy collage background module intentionally disabled.
  The public website now uses one continuous supplied background image:
  images/site-background-remodeling.png
  Kept as an inert compatibility file so existing project references/history are not broken.
*/
(() => { 'use strict'; })();
